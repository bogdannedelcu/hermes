# Copy this file to `.env` and adjust if needed. Defaults below are what production
# runs with.

HOST=127.0.0.1
PORT=8081
LOG_LEVEL=info

MAX_CONTEXT=2048
MAX_BATCH_SIZE=32
REQUEST_TIMEOUT=180

HF_HOME=/var/www/.cache/huggingface
